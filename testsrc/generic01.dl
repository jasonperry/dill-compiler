(* simplest generic test *)

(* module generic01 is *)

open stdio;

proc id(x: 't) -> 't
do
   return x;
/proc

proc main()
do
   var n = 42;
   var s = "hello";
   printString(id(s));
   printInt(id(n));
/proc

(* /module *)