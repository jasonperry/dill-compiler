(* typename format error *)

module thisname is

(* type name should be uppercase *)
type lowertype is record
   x: Int,
   y: Float;
   
proc double(x: Float) => Float
is
   return x * 2;
/proc

/module